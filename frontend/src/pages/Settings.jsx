// TODO (Phase 2): wire this page up to its backend endpoint(s) and build out
// the real UI (tables/forms/charts as specified in the product brief).
export default function Settings() {
  return (
    <div className="page">
      <h1>Settings</h1>
      <p className="page-placeholder">This section is scaffolded and ready to be built out next.</p>
    </div>
  );
}
